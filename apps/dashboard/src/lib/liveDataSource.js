/**
 * Live Supabase implementation — stubbed until Phase 6. Method signatures
 * must match mockDataSource.js exactly so dataSource.js can delegate to
 * either one with zero caller-side changes.
 */

export async function listApplicants() {
  throw new Error('liveDataSource.listApplicants: not implemented (Phase 6)')
}

export async function getApplicant(_id) {
  throw new Error('liveDataSource.getApplicant: not implemented (Phase 6)')
}
