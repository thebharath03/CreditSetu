{
  "installCommand": "npm install",
  "buildCommand": "if [ -f apps/dashboard/package.json ]; then npm run build --workspace=apps/dashboard && cp -r apps/dashboard/dist ./dist; else npm run build; fi",
  "outputDirectory": "dist"
}
